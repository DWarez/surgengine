{
    "version": "0.2.0",
    "configurations": [
        {
    "name": "Debug Simple",
    "type": "cppdbg", 
    "request": "launch",
    "program": "${workspaceFolder}/build/tests/tensor_test",
    "args": ["--gtest_filter=TensorTest.ViewReshape"],
    "stopAtEntry": true,
    "cwd": "${workspaceFolder}",
    "MIMode": "gdb",
    "setupCommands": [
        {
            "text": "set stop-on-solib-events 0"
        }
    ]
}
    ]
}